{
  "description": "The CI pipeline configuration in this repository is currently broken, so the validation flow fails. Investigate the repository workflow/pipeline files, identify the misconfiguration, and fix it so the CI flow can complete successfully. Prefer configuration/workflow fixes over application code changes unless absolutely necessary.",
  "expectedOutcome": "After your fix, the repository CI workflow configuration is valid and the pipeline can run successfully with dependencies installed before checks and build steps.",
  "bugFilePath": ".github/workflows/ci.yml"
}
